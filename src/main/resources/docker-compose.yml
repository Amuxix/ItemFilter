version: '2.0'

services:
  postgres:
    image:  library/postgres:11
    ports:
    - "5432:5432"
    environment:
      POSTGRES_USER: itemfilter
      POSTGRES_PASSWORD: itemfilter
      POSTGRES_DB: itemfilter